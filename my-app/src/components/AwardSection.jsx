import React from 'react';

const AwardSection = ({ name, description, imageUrl }) => {
  return (
    {/*
    <section className="container award-section contact-section">
      <div className="row align-items-start">
        <div className="col-md-6 p-5">
          <h2 className="mb-2">{name || "Award Name"}</h2>
          <p>{description || "This is a placeholder description for the award."}</p>
        </div>
        <div className="col-md-6">
          <img
            src={imageUrl || "https://www.computing.co.uk/news/4145723/media_19bb071f172410b83a049929a6a59bb49e20aa3db.jpeg?width=750&format=jpeg&optimize=medium"}
            alt={name || "Award Image"}
            className="img-fluid rounded"
          />
        </div>
      </div>
    </section>
  */}
  );
};

export default AwardSection;
